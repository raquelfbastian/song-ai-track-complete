server.port=8080

# ── LLM Configuration ──────────────────────────────────────────────────
# Swap provider by changing llm.provider: openai | anthropic | azure
llm.provider=openai
llm.api-key=${LLM_API_KEY:your-api-key-here}
llm.model=gpt-4o

# If using Anthropic Claude:
# llm.provider=anthropic
# llm.model=claude-3-5-sonnet-20241022

# If using Azure OpenAI:
# llm.provider=azure
# llm.azure.endpoint=${AZURE_OPENAI_ENDPOINT}
# llm.azure.deployment=${AZURE_OPENAI_DEPLOYMENT}

# ── App ────────────────────────────────────────────────────────────────
spring.application.name=kapeko
catalog.output-path=data/catalog.json

# ── CORS ───────────────────────────────────────────────────────────────
cors.allowed-origins=http://localhost:5173,http://localhost:3000
